var Type = require('type-of-is');


exports.check = function(array)
{
    var arrayTypes = [];
    for (var i = 0; i < array.length; i++)
    {
        arrayTypes.push(Type.string(array[i]));
    }
    return arrayTypes;
}
